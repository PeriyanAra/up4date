import 'package:flutter/material.dart';

class WBox extends SizedBox {
  const WBox({
    super.key,
    required double width,
  }) : super(
          width: width,
          height: 0.0,
        );
}
