import 'package:flutter/material.dart';

class HBox extends SizedBox {
  const HBox({
    super.key,
    required double height,
  }) : super(
          width: 0.0,
          height: height,
        );
}
